const buttonType = {
  primaryType: 'primary',
  secondaryType: 'secondary',
  linkType: 'link',
};

const inputType = {
  textType: 'text',
  passwordType: 'password',
};

export { buttonType, inputType };
